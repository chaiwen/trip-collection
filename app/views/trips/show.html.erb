<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @trip.title %>
</p>

<p>
  <strong>Rating:</strong>
  <%= @trip.rating %>
</p>

<p>
  <strong>Description:</strong>
  <%= @trip.description %>
</p>

<p>
  <strong>Date:</strong>
  <%= @trip.date %>
</p>



      <% if @trip.activities.any? %>
        <h3>activities</h3>

        <% @trip.activities.each do |t| %>
          <ol class="trips">
          <%= link_to "[ #{t.name} ]", t %>
          <%= t.description %>
          
          </ol>
        <% end %>
      <% end %>





<%= link_to 'Edit', edit_trip_path(@trip) %> |
<%= link_to 'Back', trips_path %>
